using System;
using System.Windows.Forms;

namespace DynamicSepticSystem
{
    public partial class FormProgresoActualizacion : Form
    {
        public FormProgresoActualizacion()
        {
            InitializeComponent();
            ThemeManager.AplicarTema(this);
        }

        // Puedes añadir lógica adicional (event handlers) aquí si es necesario.
    }
}
